{
  "name": "qz-agent",
  "version": "1.0.0",
  "description": "CLI for registering agents with QueryZero and obtaining BBS+ credentials via wallet signing",
  "license": "MIT",
  "type": "module",
  "bin": {
    "qz-agent": "./src/cli.ts"
  },
  "scripts": {
    "start": "bun src/cli.ts",
    "build": "bun build src/cli.ts --compile --outfile dist/qz-agent"
  },
  "dependencies": {
    "@digitalbazaar/bbs-signatures": "3.0.0",
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
